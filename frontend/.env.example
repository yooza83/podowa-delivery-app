# TMap 지도 표시용(JS SDK) 키. 브라우저 화면 소스에 그대로 노출되는 '공개용' 값이라
# 프론트엔드 빌드 시점에 HTML에 심어진다(백엔드의 TMAP_REST_APP_KEY와는 별개 위치).
# 같은 앱의 키를 그대로 써도 된다 — backend/.env의 TMAP_REST_APP_KEY와 동일한 값 가능.
VITE_TMAP_JS_APP_KEY=
